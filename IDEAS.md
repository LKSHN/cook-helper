# Ideas

Drop anything here — one line or a whole paragraph, doesn't need to be polished.
When you want one built, just point me at it (or say "build the next one").

- New tab feature: Add tabs at the top so we have a "recap" tab that will contain the tool we've developed so far, and a "mep" tab that will contain a tool to help me do my setup list (we'll work on it on an another topic)

- MEP Tab: it should be divided in two mode: before and after a service
    -before: it should be a list of every ingredients that needs to be prepared before the service, this list will be filled using the after mode
    -after: a list containing all the ingredients of all recipes with a way to add them to the before list

- Recap interface: 
    - move the bottom bar to the top, below the header
    - instead of filtering the recipes, we'll just have all the recipes on one list but sorted by station and ordered by highest ingredients number

## Done
(move items here once built, so the list above stays current)

- Interface changes: Change the edit menu so it fits more the rest of the interface — matched the form's header bar to the main "THE RAIL" header (flat background, bottom border, edge-to-edge)

- Colors: Add in the edit menu a way to change colors of an ingredient when expending a recipe — each ingredient row has a swatch button opening an 8-color palette; the color shows as a dot next to the name in both the accordion and full recipe view

- Remove a tab: remove the "sauce & dressing" tab — dropped from filter tabs and the form's station dropdown
